# This project now runs 100% free using Ollama (a local LLM) - no API key needed!
# These settings are optional - defaults already match a standard Ollama install.

# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.1:8b
